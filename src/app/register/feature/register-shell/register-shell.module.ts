import {NgModule} from '@angular/core';
import {RegisterShellRoutingModule} from "./register-shell-routing.module";

@NgModule({
  imports: [
    RegisterShellRoutingModule
  ]
})
export class RegisterShellModule {
}
